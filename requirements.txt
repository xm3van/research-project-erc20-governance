# --- Core data + environment ---
numpy>=1.24,<3
pandas>=2.0,<3
python-dotenv>=1.0,<2
requests>=2.31,<3
web3>=6,<7

# --- Graphs / networks ---
networkx>=3.0,<4
python-louvain>=0.16  # Louvain community detection

# --- Optional large-data support (comment out if pure pandas) ---
# dask[dataframe]>=2024.1.0

# --- Progress + utilities ---
tqdm>=4.66,<5

# --- Analysis / statistics / plotting ---
scipy>=1.10,<2
statsmodels>=0.14,<0.15
matplotlib>=3.7,<4
seaborn>=0.12,<1
pyarrow>=14,<18       # only needed if you read/write Parquet files

# --- Interactive / notebooks ---
ipython>=8.12,<8.19   # 8.18.1 is last line that supports Python 3.9
ipykernel>=6.29,<7
